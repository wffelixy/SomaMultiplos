# Soma dos Múltiplos de 3 ou 5

Este é um simples programa em Java que realiza a soma de todos os números que são múltiplos de 3 ou 5 até um determinado número fornecido como parâmetro.

## Funcionalidades

- Calcula a soma de todos os números que são múltiplos de 3 ou 5 até um número fornecido.
- Aceita um número inteiro `x` como entrada e retorna a soma.

## Pré-requisitos

- Java Development Kit (JDK) instalado
- IDE Java de sua preferência (ex: Eclipse, IntelliJ)

## Como executar

1. Clone ou faça o download deste repositório.

2. Abra o projeto em sua IDE Java.

3. Execute a classe `Main.java` localizada em `somaMultiplos.Main`.

4. Insira um número inteiro quando solicitado. Nesse projeto o número 10 já está fixo no código.

5. A soma dos múltiplos de 3 ou 5 até o número fornecido será exibida na saída do console.


